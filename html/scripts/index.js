// Generated by CoffeeScript 1.6.3
(function() {
  var LOG, shortSpan, startTime, summary, timer, timerPeriod, updatePeriod, updater;

  this.GLOB = new Common;

  LOG = null;

  updatePeriod = 5000;

  timerPeriod = 100;

  startTime = 10;

  updater = new LogUpdater;

  summary = new LogSummary;

  shortSpan = new ShortSpan;

  timer = new Timer;

  timer.setCallback(function() {
    return shortSpan.show(LOG, startTime, 0.001 * timerPeriod);
  });

  $(function() {
    summary.buildSummaryArea("#out2");
    shortSpan.buildDrawArea("#out2");
    updater.init();
    updater.onUpdate(function(log) {
      LOG = log;
      return summary.show(LOG);
    });
    $("#btnStart0").click(function() {
      updater.start(updatePeriod);
      return timer.start(timerPeriod);
    });
    $("#btnStop0").click(function() {
      updater.stop();
      return timer.stop();
    });
    return $("#sldFreq").change(function() {
      var fr;
      fr = $("#sldFreq").val();
      timerPeriod = 1000.0 / fr;
      timer.stop();
      return timer.start(timerPeriod);
    });
  });

}).call(this);

/*
//@ sourceMappingURL=index.map
*/
